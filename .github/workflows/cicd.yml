name: CI/CD test
run-name: ${{ github.actor }} is running CI/CD 🚀
on: [workflow_dispatch]
jobs:
  # Setup:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Install gcc
  #       run: |
  #        sudo apt update
  #        sudo apt install gcc-13
  #     - uses: actions/checkout@v4
  #     - name: Use Node.js
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: '18.x'
  #     - name: Install dependencies
  #       run: npm ci
  #     - name: Build selva
  #       run: |
  #         cd selvad
  #         make
  #         make install
  #         cd ..
  #       env:
  #        CC: gcc-13
  #     - name: Build based-db
  #       run: npm run build
  #     - name: Run client tests
  #       run: |
  #         cd packages/client
  #         npm run test-ci
  BuildSelva:
    runs-on: ubuntu-latest
    steps:
      - name: Install gcc
        run: |
         sudo apt update
         sudo apt install gcc-13
      - uses: actions/checkout@v4
      - name: Build selva
        run: |
          cd selvad
          make
          make install
          cd ..
        env:
         CC: gcc-13
      # - name: Archive selva binaries
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: selvad-build
      #     path: |
      #       selvad/binaries/Linux_x86_64
      #       selvad/lib/**/*.so
      #       selvad/modules/**/*.so
      #       selvad/locale
      #       selvad/selvad
      #         tar -cvf selva_build.tar selvad/binaries/Linux_x86_64 selvad/lib/**/*.so selvad/modules/**/*.so selvad/locale selvad/selvad
      - name: Tar selva binaries
        run: |
          tar -cvf selva_build.tar selvad/lib/**/*.so
          find selvad/ -name "*.so*" -type l
          find selvad/ -name "*.so*" -type l | xargs tar -cvfh selva_build_linked_files.tar
      - name: Archive selva binaries tar
        uses: actions/upload-artifact@v3
        with:
          name: selvad-build-archived
          path: |
            selva_build.tar
            selva_build_linked_files.tar
  SchemaTests:
    needs: BuildSelva
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'
      - name: Download selva binaries tar
        uses: actions/download-artifact@v3
        with:
          name: selvad-build-archived
          path: selvad/
      - name: Untar selva binaries
        run: |
          tar -xvf selva_build.tar
          tar -xvfh selva_build_linked_files.tar
          ls -la
          la -la selvad
      - name: Setup selvad permissions
        run: chmod +x selvad/selvad
      - name: Install dependencies
        run: npm ci
      - name: Build based-db
        run: npm run build
      - name: Run client schema tests
        run: |
          cd packages/client
          npm run test-ci -- test/schema/*.ts

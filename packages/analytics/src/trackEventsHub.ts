// for event ids do we want to send those back?
// or just send the strings here (saves a lot of resync stuff)

// listener on hubs disocnnect on db server
// substracts all current actives
// type to keep track of hubs (with active per event)

// batch for hub
// protocol will change (but can do later)

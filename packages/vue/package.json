{
  "name": "@based/vue",
  "version": "2.4.1",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "watch": "npm run build -- --watch",
    "clean": "rimraf {.turbo,dist,node_modules}"
  },
  "peerDependencies": {
    "vue": "^3.0.0",
    "@based/client": "^2.8.0"
  },
  "devDependencies": {
    "vue": "^3.0.0",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5",
    "rimraf": "^3.0.2"
  }
}

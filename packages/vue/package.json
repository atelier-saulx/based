{
  "name": "@based/vue",
  "version": "3.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "watch": "npm run build -- --watch",
    "clean": "rimraf {.turbo,dist,node_modules}"
  },
  "peerDependencies": {
    "vue": "^3.0.0",
    "@based/client": "^3.0.0"
  },
  "devDependencies": {
    "@based/client": "*",
    "vue": "^3.0.0",
    "ts-node": "10.9.1",
    "typescript": "^4.3.5",
    "rimraf": "^3.0.2"
  }
}

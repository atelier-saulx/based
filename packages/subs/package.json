{
  "name": "@based/db-subs",
  "version": "3.0.0-alpha.1",
  "license": "MIT",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "npx tsc",
    "watch": "npm run build -- --watch",
    "clean": "rimraf {.turbo,dist,node_modules}",
    "test": "FORCE_COLOR=1 ava --color --verbose",
    "test-ci": "FORCE_COLOR=1 ava --color --concurrency 1"
  },
  "files": [
    "dist/src",
    "!dist/src/**/*.js.map"
  ],
  "sideEffects": false,
  "type": "module",
  "ava": {
    "concurrency": 1,
    "timeout": "30s",
    "files": [
      "./dist/test/*.js"
    ]
  },
  "dependencies": {
    "@based/db-client": "^3.0.0"
  },
  "devDependencies": {
    "@based/db-server": "*",
    "get-port": "^5.1.1",
    "@types/node": "^17.0.23",
    "ava": "6.1.1",
    "ts-node": "10.9.1",
    "typescript": "^5.1.6",
    "rimraf": "^3.0.2"
  }
}

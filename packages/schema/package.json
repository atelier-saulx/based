{
  "name": "@based/schema",
  "version": "4.0.0-alpha.0",
  "license": "MIT",
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md",
    "package.json",
    "!dist/**/*.map"
  ],
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "tsc && tsc test/*.ts --noEmit && tsx --test test/*.ts"
  },
  "sideEffects": false,
  "type": "module",
  "devDependencies": {
    "@saulx/tsconfig": "^1.1.0",
    "@types/node": "22.5.3",
    "tsx": "^4.19.0",
    "typescript": "^5.1.6"
  }
}

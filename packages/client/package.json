{
  "name": "@based/db-client",
  "version": "0.0.1",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "watch": "npm run build -- --watch",
    "clean": "rimraf {.turbo,dist,node_modules}",
    "test": "SAVE_AT_EXIT=0 ava --color --node-arguments='--expose-gc' --timeout 3m --verbose"
  },
  "sideEffects": false,
  "ava": {
    "files": [
      "test/*.ts"
    ],
    "concurrency": 1,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  },
  "dependencies": {
    "@saulx/diff": "^1.1.4",
    "@saulx/utils": "^3.5.3",
    "@based/client": "^4.7.6",
    "@based/schema": "1.1.2",
    "data-record": "^1.0.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "ava": "3.15.0",
    "ts-node": "10.9.1",
    "typescript": "^5.1.6",
    "rimraf": "^3.0.2",
    "before-exit": "1.0.0",
    "@saulx/hash": "^2.0.0",
    "@types/uuid": "^9.0.2"
  }
}

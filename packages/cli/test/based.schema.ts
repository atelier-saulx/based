// import { schema } from '@based/schema'

export default {
  types: {
    x: {
      x: 'string',
      y: 'text',
    },
    flap: {
      flap: 'string',
    },
  },
}

// .infer, .schema
// type Flap = typeof schema.infer['flap']

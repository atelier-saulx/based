module.exports = {
  name: 'hello-world-observable',
  observable: true,
  // publish: false
  // shared: true / false
  // main: // path
  // event / name
  // type: 'event' | 'job' | 'function'
  // trigger: // path OR json (inline) OR query object
}

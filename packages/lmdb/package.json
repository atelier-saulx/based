{
  "name": "@based/lmdb",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "main": "./dist/src/index.js",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "watch": "npx tsc --watch",
    "test": "npx ava",
    "clean": "rimraf {.turbo,dist,node_modules}"
  },
  "sideEffects": false,
  "ava": {
    "timeout": "2m",
    "workerThreads": false,
    "files": [
      "./dist/test/**/*.js"
    ]
  },
  "files": [
    "dist/src",
    "README.md",
    "package.json"
  ],
  "dependencies": {
    "node-lmdb": "^0.10.0",
    "@based/server": "^8.0.0",
    "@based/client": "^6.0.0"
  },
  "devDependencies": {
    "ts-node": "10.9.1",
    "ava": "5.3.1",
    "typescript": "^4.3.5",
    "rimraf": "^3.0.2"
  }
}

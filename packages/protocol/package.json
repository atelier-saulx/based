{
  "name": "@based/protocol",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npx tsc",
    "watch": "npm run build -- --watch",
    "test": "ava --timeout 1m --verbose",
    "watchTest": "ava --color --watch --verbose",
    "clean": "rimraf {.turbo,dist,node_modules}"
  },
  "sideEffects": false,
  "main": "dist/index.js",
  "browser": "dist/decode.js",
  "module": "src/index.ts",
  "dependencies": {
    "@based/types": "^2.6.0",
    "fflate": "^0.7.3"
  },
  "ava": {
    "timeout": "60s",
    "files": [
      "test/*.ts"
    ],
    "concurrency": 1,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5",
    "ava": "3.15.0",
    "rimraf": "^3.0.2"
  }
}

console.log('box')

export default () => {}

// colish
//   if (walkable) {
//     const minDistance = 5
//     for (const b of blocks) {
//       if (
//         block.x < b.x + b.w + minDistance &&
//         // Is the right edge of 'block' within (left edge of 'b' - minDistance)?
//         block.x + block.w > b.x - minDistance &&
//         // Check Y-axis proximity:
//         // Is the top edge of 'block' within (bottom edge of 'b' + minDistance)?
//         block.y < b.y + b.h + minDistance &&
//         // Is the bottom edge of 'block' within (top edge of 'b' - minDistance)?
//         block.y + block.h > b.y - minDistance
//       ) {
//         return block
//       } else {
//         // return block
//       }
//     }
//   }

{
  "name": "@based/templates",
  "version": "0.1.0",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc && npm run build-functions",
    "watch": "npm run build -- --watch",
    "clean": "rimraf {.turbo,dist,node_modules}",
    "build-functions": "node ./scripts/buildFunctions.js"
  },
  "sideEffects": false,
  "ava": {
    "files": [
      "test/*.ts"
    ],
    "concurrency": 1,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  },
  "devDependencies": {
    "esbuild": "^0.14.29",
    "@types/node": "^17.0.25",
    "ava": "3.15.0",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5",
    "rimraf": "^3.0.2"
  }
}

const readInt = @import("../../utils.zig").readInt;

pub inline fn compare(
    size: u16,
    op: u8,
    value: []u8,
    values: []u8,
) bool {
    if (size == 4) {
        // return readInt((u32, ))
    }

    return false;
}

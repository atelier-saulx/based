{
  "name": "exporter",
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -Rf node_modules dist",
    "test": "tsc -b && node dist/index.js",
    "test-watch": "tsc --watch & node --watch dist/index.js",
    "watch": "tsc -b --watch",
    "build": "tsc -b",
    "start": "node dist/index.js"
  },
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "source": "src/index.ts",
  "sideEffects": false,
  "keywords": [
    "based",
    "export",
    "exporter",
    "dump",
    "parquet",
    "csv"
  ],
  "description": "Exports Based DB into other formats.",
  "prettier": "@saulx/prettier-config",
  "devDependencies": {
    "@saulx/prettier-config": "^2.0.0",
    "@saulx/tsconfig": "^1.1.3",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@types/node": "^24.3.0"
  }
}

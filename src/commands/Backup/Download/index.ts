import { Command } from 'commander'
import { BasedClient } from '@based/client'
import { basedAuth } from '../../../shared/index.js'

export const download = async (program: Command) => {
  // const basedClient: BasedClient = await basedAuth(program)
}

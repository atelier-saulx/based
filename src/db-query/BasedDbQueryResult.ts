// const r = new BasedQueryResult(READ_SCHEMA, ?result)
// r.update(result)
// r.inspect()

// node getter
// BASED-RESPONSE
// { data, error, checksum }
// .data ->
// for (const node of response) {}
// .map
// .id

// length
// map
// iterator

/*
    <div>{useQuery().map(v => <div>{v.title}</div>)}</div>
*/
